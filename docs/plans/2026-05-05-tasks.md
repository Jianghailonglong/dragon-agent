# Dragon Agent 实施任务清单

> 基于 `2026-05-05-dragon-agent-design.md` 拆分，共 5 Phase / 35 子任务。

---

## Phase 1：Core（核心可运行）

- [x] 1.1 项目骨架搭建（目录结构、pyproject.toml、config/schema）
- [x] 1.2 `AgentRunSpec` + `AgentRunResult` 数据类
- [x] 1.3 `AgentHook` 生命周期基类 + `CompositeHook`
- [x] 1.4 `ToolRegistry` + `BaseTool` ABC + 3 个基础工具（read_file, exec, ask_user）
- [x] 1.5 `AgentRunner` 纯执行引擎（LLM 调用 → 工具分发循环）
- [x] 1.6 `LLMProvider` ABC + `AnthropicProvider` 实现 + `ProviderFactory`
- [x] 1.7 `AgentLoop` 顶层编排器（消费消息 → 编排 Runner）
- [x] 1.8 基础 Session（内存版）
- [x] 1.9 CLI Channel（`BaseChannel` ABC + `CLIChannel`）
- [x] 1.10 CLI 入口 `cli/main.py`，端到端跑通

**里程碑**：CLI 下可以和 agent 对话，agent 能调用工具

---

## Phase 2：Session + Intelligence（会话 + 智能）

- [x] 2.1 JSONL 持久化 `Session` + `SessionManager`（save/load/replay）
- [x] 2.2 崩溃恢复机制（pending_turn + runtime_checkpoint）
- [x] 2.3 上下文溢出 3 层防御：Microcompact → Tool Result Budget → Snip History
- [x] 2.4 `AutoCompact`（idle 30 分钟自动压缩）+ `Consolidator`（LLM 摘要）
- [x] 2.5 `ContextBuilder` 8 层 prompt 组装
- [x] 2.6 `MemoryStore`（MEMORY.md + history.jsonl）
- [x] 2.7 `SkillsLoader`（SKILL.md 自动发现）
- [x] 2.8 更多工具：write_file, edit_file, grep, glob, web_search, web_fetch
- [x] 2.9 `InboundMessage` / `OutboundMessage` 统一消息类型（Phase 1 已完成）

**里程碑**：agent 有记忆、有技能、会话可持久化

---

## Phase 3：Gateway + Channels（网关 + 通道）

- [x] 3.1 `MessageBus`（inbound/outbound 队列）
- [x] 3.2 `GatewayRouter` 5-tier 路由（peer > guild > account > channel > default）
- [x] 3.3 `ChannelManager` 通道注册管理
- [x] 3.4 `TelegramChannel` 实现
- [x] 3.5 `FeishuChannel` 实现
- [x] 3.6 `WeChatChannel` 实现
- [x] 3.7 `bindings.yaml` 配置加载 + 路由集成

**里程碑**：多通道接入，多 agent 路由

---

## Phase 4：Resilience（韧性）

- [x] 4.1 `Checkpoint` 恢复机制完善（持久化到 session metadata）
- [x] 4.2 3-Layer Retry Onion（工具自纠正 → 紧急压缩 → key 轮换 + backoff）
- [x] 4.3 `DeliveryQueue` Write-Ahead 投递队列
- [x] 4.4 `ToolSafetyGuard`（workspace 边界、SSRF 防护、重复查询限制）
- [x] 4.5 Mid-turn Injection 机制（`_pending_queues` 注入）

**里程碑**：崩溃恢复、重试容错

---

## Phase 5：Orchestration（编排）

- [x] 5.1 `LaneQueue` Named Lanes（main / cron / heartbeat 优先级）
- [x] 5.2 `HeartbeatService` 定时检查
- [x] 5.3 `CronScheduler` 定时任务
- [x] 5.4 `SubagentManager` 子 agent 并行
- [x] 5.5 `Dream` 自动记忆提取（后台扫描 → MEMORY.md）

**里程碑**：主动 agent、子 agent 并行
